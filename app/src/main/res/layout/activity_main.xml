<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="com.managerlee.assessment.viewModel.UpdateViewModel"/>
        <variable
            name="viewModel"
            type="com.managerlee.assessment.viewModel.UpdateViewModel"/>
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/welcome"
        android:gravity="center"
        android:orientation="vertical">
        <ImageView
            android:layout_width="@dimen/width200"
            android:layout_height="@dimen/height200"
            android:src="@drawable/welcome_logo" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/top10"
            android:text="@string/app_name"
            android:textColor="@android:color/white"
            android:textSize="@dimen/textSize18"
            android:textStyle="bold" />
    </LinearLayout>
</layout>
