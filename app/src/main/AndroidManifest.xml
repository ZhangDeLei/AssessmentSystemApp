<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.managerlee.assessment">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".framework.config.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.LoginActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.IndexActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.ArticleSubmissionActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.ArticleSubmissionHisActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.SearchActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.AccountBindActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.SettingActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.TaskCommentActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.TaskDetailActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".view.TaskReleaseActivity" android:theme="@style/AppTheme"/>
    </application>

</manifest>